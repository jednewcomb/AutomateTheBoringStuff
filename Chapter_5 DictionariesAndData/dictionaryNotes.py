# keys(), values(), items() methods 
# get() takes two args, the key of the value to retrieve and a fallback value if it doesn't exist (like getOrDefault in java)
# setdefault() First argument passed to the method is the key to check for, 
#              second argument is the value to set at that key if the key does not exist. 
#              If the key does exist, the setdefault() method returns the key’s value

# import pprint for pretty printing:
# pprint.pprint(someDictValue)